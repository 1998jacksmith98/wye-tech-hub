/** @deprecated Import from `@/lib/storage` — kept for existing imports. */
export {
  StorageConfigError,
  getStorageStatus,
  resolveStorageProvider,
  storeUpload,
} from "./storage";
export type {
  StorageProvider,
  StorageStatus,
  StoreUploadParams,
  UploadResult,
} from "./storage";
